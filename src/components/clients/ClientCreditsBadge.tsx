"use client";

/** Badge crédits retiré — tarification par abonnement + devis personnalisé. */
export function ClientCreditsBadge(_props: {
  clientId: string;
  compact?: boolean;
  className?: string;
}) {
  return null;
}
