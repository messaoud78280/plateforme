import { TrainingOffersSection } from "@/components/marketing/TrainingOffersSection";

/** Tarif /formation — même parcours 7 h / 14 h que la homepage. */
export function FormationPricing() {
  return <TrainingOffersSection id="tarif" analyticsPrefix="formation-tarif" />;
}
