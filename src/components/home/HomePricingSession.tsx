import { TrainingOffersSection } from "@/components/marketing/TrainingOffersSection";

/** Section tarifs homepage — parcours 7 h / 14 h. */
export function HomePricingSession() {
  return <TrainingOffersSection id="tarif" analyticsPrefix="home-tarif" />;
}
