/**
 * GED V2 — snapshot PDF devis accepté (même storageKey, pas de copie).
 * Délègue à l’indexeur V2.0.1 (upgrade d’une entrée virtuelle si elle existe).
 */
export { ingestAcceptedQuoteSnapshot } from "@/lib/ged/ingest-commercial-document";
