/** @deprecated Importer depuis `@/lib/schema` — réexport de compatibilité. */
export {
  buildResourcesHubCollectionJsonLd,
  buildServiceOfferingJsonLd,
  buildServicesHubItemListJsonLd,
} from "@/lib/schema";
